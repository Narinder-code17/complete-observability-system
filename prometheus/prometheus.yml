global:
  scrape_interval: 15s

  external_labels:
    project: "complete-observability-system"

scrape_configs:
  - job_name: "observability-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "app:5000"